// Used during production build (ng build)
// Replace the URL below with your actual Render backend URL after deployment.
export const environment = {
  production: true,
  apiUrl: 'https://sti-ai.onrender.com',
};
